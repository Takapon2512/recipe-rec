# プロジェクト概要

家庭用 在庫管理・献立支援システム。
- FE: Next.js 14+ / TypeScript / Vercel
- BE: Go / EC2 (t4g.nano)
- 認証: Cognito (JWT検証)
- DB: RDS MySQL 8.0

## レビュー観点(優先度順)

### 1. セキュリティ
- JWT検証ロジックの抜け漏れ(issuer/audience/exp すべて検証されているか)
- SQLインジェクション対策(プレースホルダ使用)
- 認可漏れ(自分のリソースのみ操作可能か、`users.cognito_sub` で突合しているか)
- 秘密情報のハードコード

### 2. APIコントラクト
- `docs/api.md` の共通レスポンスエンベロープ(`{ "data": ... }`)に準拠しているか
- エラーレスポンスが `{ "error": { "code", "message" } }` 形式か
- 他人のリソース参照時に 403 ではなく 404 を返しているか

### 3. データ整合性
- `deleted_at` での論理削除を尊重しているか
- トランザクション境界が適切か
- N+1クエリ

### 4. コード品質
- Goは `errors.Is` / `errors.As` での比較、`fmt.Errorf` で `%w` ラップ
- TypeScriptは `any` を避ける
- 命名、責務分離

## レビューしないこと
- 既存コードのスタイルに合わせていれば、好みのスタイル指摘は不要
- フォーマット(linterが見るため)

## 言語
レビューコメントは **日本語** で書いてください。
