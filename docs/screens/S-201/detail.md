## S-201 在庫一覧
 
**URL**: `/inventory`
**Figma**: [{TBD}]({TBD})
 
### 🎯 目的
- いま家にある食材・日用品を一覧で確認する
- 賞味期限切れが近いものを把握する
- 「使い切った」「1個使った」を素早く記録する
- 検索・絞り込みで目的の在庫を見つける
### 📋 構成要素
 
#### ヘッダー(簡略、48px / 共通仕様準拠)
タイトル省略。アクションアイコンのみ右寄せに配置。
 
| パーツ | 配置 | 内容 |
|-------|------|------|
| (空) | 左 | タイトル省略(BottomNavで「在庫」が選択中なので明示不要) |
| 検索アイコン | 右 | タップで検索バー展開 |
| ソートアイコン | 右 | タップでソートメニュー |
 
#### チップフィルタ（ヘッダー下、横スクロール）
- カテゴリ（野菜 / 肉 / 魚 / 乳製品 / 調味料 ...）
- 保管場所（冷蔵 / 冷凍 / 常温）
- **期限間近のみ**（トグル、特に重要）
複数選択可。選択中のチップは塗りつぶし表示。
 
#### 在庫リスト（メインコンテンツ）
**標準カード**（高さ約80px、2行構成）：
 
```
┌────────────────────────────────────┐
│ ●  鶏もも肉                         │
│    あと2日 / 300g · 冷蔵 · 肉      │
└────────────────────────────────────┘
```
 
| 要素 | 詳細 |
|------|------|
| ●（左ドット） | 期限なし=灰、期限〜3日=黄、期限切れ=赤 |
| 1行目（大） | 商品名（16px / 600） |
| 2行目（小） | 期限テキスト + 数量・単位 + 保管場所 + カテゴリ（13px / 400、`text-secondary` 色） |
| 期限テキスト | 期限間近=黄、期限切れ=赤、それ以外=`text-secondary` |
 
#### フローティングアクションボタン（FAB、右下）
- アイコン：`+`
- タップで `/inventory/new`（S-203）へ
- スクロール時も常時表示
#### ページネーション（リスト下部）
- `< 1 2 3 ... 5 >` シンプル形式
- 1ページあたり20件（API既定値）
### 🔄 インタラクション
 
#### スワイプ操作
 
| 方向 | アクション | API | フィードバック |
|------|----------|-----|--------------|
| ←（左） | 使い切った | `DELETE /api/inventory/{id}` | カードがリストから消える + トーストに **「取り消し」付き**（5秒） |
| →（右） | 1個使う | `PATCH /api/inventory/{id}` (quantity -= 1) | カード内の数量が即時更新（トーストなし） |
 
**スワイプ実装の詳細**：
- スワイプ閾値：カード幅の30%
- スワイプ中は背景色（左=赤、右=青）とアイコンが見える
- 閾値未満で離した場合はカードがバウンドして元に戻る
- アクション完了時のアニメーション：左スワイプはカードがフェードアウトして消える、右スワイプは数量数字が一瞬ハイライト
#### スワイプ取り消しの仕様
- トースト：「鶏もも肉を削除しました」「取り消し」ボタン付き
- 取り消し操作：論理削除フラグを戻す（`deleted_at = NULL` への復元 API）
- タイムアウト：5秒で自動消滅
- 連続して別のカードを削除した場合：直近のトーストは閉じ、新しいトーストに置き換わる（同時に複数表示しない）
#### タップ操作
- カード本体タップ → 詳細画面（S-202）へ
- 検索アイコンタップ → 検索バー展開
- ソートアイコンタップ → ソートメニュー
#### 検索バー
- 検索アイコンタップで上部にバーが下りてくる（高さ48px、150ms ease-out）
- バー表示中はチップフィルタが隠れる
- 入力中は **300msデバウンス** でAPI呼び出し
- `×` ボタンで入力クリア、`キャンセル` ボタンでバーを閉じる
#### ソートメニュー
- アクションシート風（下から出現）
- 選択肢：
  1. **期限近い順**（既定）
  2. 追加日が新しい順
  3. 名前順（あいうえお）
  4. カテゴリ別グループ表示
- 選択中の項目にチェックマーク
#### プルダウンリフレッシュ
- リスト最上部からプルダウンでリロード
- スピナーは標準的なネイティブ風
#### 自動リフレッシュ
- 画面フォーカス時（タブ切替、画面復帰）に自動再取得
- TanStack Query の `refetchOnWindowFocus: true` 相当
- ローディング表示は控えめに（既存リストを残したまま、背景でリフレッシュ）
### 📡 データ・API
 
```
GET /api/inventory?page=1&per_page=20&sort=expires_at&category_id=&storage_location=&q=
PATCH /api/inventory/{id}      # 1個使う：quantity -= 1
DELETE /api/inventory/{id}     # 使い切った：論理削除
POST /api/inventory/{id}/restore   # 取り消し（要追加）
```
 
### 🌀 状態
 
| 状態 | 表示 |
|------|------|
| `loading`（初回） | スケルトンカード5枚 |
| `loading`（再取得） | 既存リスト維持、最上部に細いプログレスバー |
| `loaded` | 通常表示 |
| `empty` | イラスト + 「最初の在庫を登録しよう」+ FAB強調 |
| `error` | インラインエラーバナー + 自動リトライ（3回、指数バックオフ） |
| `error after retries` | 「読み込みに失敗しました」+ 手動リトライボタン |
| `offline` | オフラインバナー（上部固定） |
 
### ⚠️ エッジケース
 
- **期限切れ**：左ドット赤、期限テキスト「3日前」のような過去日数表示（赤）。並びは通常通り
- **期限なし**：左ドット灰、期限テキスト非表示、リスト最後にまとめる
- **数量小数**：単位が `g/ml/kg/l` の場合は「1個使う」スワイプを無効化
- **残り1個で1個使う**：自動的に「使い切った」扱いに昇格
- **オフライン**：直前のキャッシュ + オフラインバナー、操作は楽観的更新
### 💭 設計判断メモ
 
| 判断 | 理由 |
|------|------|
| カードを2行（80px）構成 | 期限テキストを大きく見せる方針と整合 |
| 左ドット方式 | バッジよりノイズが少なく圧迫感がない |
| スワイプ左右で別アクション | 慣れたユーザーには最速、誤操作は取り消し機能で緩和 |
| 「使い切った」のみ取り消し付き | 破壊的操作と非破壊的操作で扱いを分ける |
| 期限切れも同じリストに | 緊急度の強調しすぎを避け、色での連続的な表現 |
| 期限なしを最後にまとめる | 期限管理の世界観として「期限のあるもの」を優先 |
| ページネーション採用 | 多くても100件、シンプルで実装も楽 |
| 検索アイコン展開方式 | 縦スペース節約 |
| 自動リトライ3回 | 一時的な不調を吸収 |
 
### 📝 未決事項・TODO
 
- [ ] api.md に `POST /api/inventory/{id}/restore`（取り消し用）を追加
- [ ] 単位ごとのスワイプ可否ルールを定義（個 vs g/ml）
- [ ] 期限テキストの表記揺れを統一（「あと3日」「3日前」「今日」「明日」）
- [ ] スケルトンカードのデザイン詳細
- [ ] 空状態のイラスト素材