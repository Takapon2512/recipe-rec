version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - errcheck # エラーチェック漏れ
    - govet # 標準のvet
    - staticcheck # 静的解析 (v2 で gosimple を統合)
    - unused # 未使用変数
    - ineffassign # 無効な代入
